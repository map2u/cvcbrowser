<ol class="carousel-indicators">
    {% for indicatorbenefitimage in entities.indicatorbenefitimages %}
        {% set images=indicatorbenefitimage.getFileName()|unserialize %}
        {% if images|length > 0 %}
            {% for image in images %}
                <li data-target="#leftsidebar_indicator_benefit_flash" data-slide-to="{{index}}" {% if index==1 %} class="active" {% endif %}>
                </li>
                {% set index = index + 1 %}
            {% endfor %}
        {% endif %}
    {% endfor %}
</ol>
