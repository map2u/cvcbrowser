{# empty Twig template #}
<div class="container-fluid" style="height:300px;padding-bottom: 10px;margin-bottom: 10px;border-bottom: 2px   #090 ridge;">
    <div class="row-fluid">
        <div class="tabbable">
            <ul class="nav nav-tabs">
                    {% set i=1 %}
                    {% for station in stations %}
                <li ><a href="#pane{{i}}" data-toggle="tab">{{station.getStationName()}}</a></li>
                    {% set i= i+1 %}
                    {% endfor %}
            </ul>
            <div class="tab-content">
                    {% set i=1 %}
                    {% for station in stations %}

                <div id="pane{{i}}" class="tab-pane " style="text-align:left;">

                    <ul>  

   

                    </ul>

                </div>
                    {% set i= i+1 %}
                    {% endfor %}
            </div>
        </div>
    </div>
</div>