<div>
    {% set audios=usergeometries.getAudio()|unserialize %}
    {% if audios|length > 0 %}
        {% for audio in audios %}
            {% if audio !=null and audio !='' %}
                <audio src="{{audio}}" controls>
                    Your browser does not support the <code>audio</code> element.
                </audio>
            {% endif %}
        {% endfor %}
    {% endif %}
</div>
